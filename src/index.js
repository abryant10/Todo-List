import './styles/styles.css';
import {
    myIcon, 
    renderListsToForm, 
    renderListView, 
    renderTaskView
} from './modules/UI';
footerLink.appendChild(myIcon);
renderListsToForm();
renderListView();
renderTaskView("all Tasks");

